<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= pageTitle %></title>
        <link rel="stylesheet" href="/styles/CSS/nav.css">
        <link rel="stylesheet" href="/styles/CSS/signup.css">
        <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
        <script src="/scripts/main.js" defer></script>
    </head>
    <body>
        <div id="backdrop"></div>
        <header>
            <%- include('../includes/nav.ejs') %>
        </header>
        <main>
            <form action="/sign-up" method="post" id="signup-form">
                <% if (errorMessage) { %>
                    <div class="user-message user-messsage--error"><%= errorMessage %></div>
                <% } %>
                <h1>Sign-up</h1>
                <div class="input-container">
                    <input 
                    class="<%= validationErrors.find(e => e.path === 'username') ? 'invalid' : '' %>"
                    type="text" name="username" id="username_text" placeholder="Username" value="<%= oldInput.username %>">
                    <i class='bx bxs-user'></i>
                </div>
                <div class="input-container">
                    <input 
                    class="<%= validationErrors.find(e => e.path === 'email') ? 'invalid' : '' %>"
                    type="text" name="email" id="email_text" placeholder="Email" value="<%= oldInput.email %>">
                    <i class='bx bxs-envelope' ></i>
                </div>
                <div class="input-container">
                    <input 
                    class="<%= validationErrors.find(e => e.path === 'password') ? 'invalid' : '' %>"
                    type="password" name="password" id="password_text" placeholder="Password" value="<%= oldInput.password %>">
                    <i class='bx bxs-lock-alt' ></i>
                </div>
                <div class="input-container">
                    <input 
                    class="<%= validationErrors.find(e => e.path === 'confirmPassword') ? 'invalid' : '' %>"
                    type="password" name="confirmPassword" id="confirm-password_text" placeholder="Confirm password" value="<%= oldInput.confirmPassword %>">
                    <i class='bx bxs-lock-alt' ></i>
                </div>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit">Sign-up</button>
                <p>
                    Already have an account? <a href="/login">Log in</a>
                </p>
            </form>
        </main>
        <footer></footer>
    </body>
</html>