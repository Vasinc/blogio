<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= pageTitle %></title>
        <link rel="stylesheet" href="/styles/CSS/nav.css">
        <link rel="stylesheet" href="/styles/CSS/login.css">
        <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
        <script src="/scripts/main.js" defer></script>
    </head>
    <body>
        <div id="backdrop"></div>
        <header>
            <%- include('../includes/nav.ejs') %>
        </header>
        <main>
            <form action="/login" method="post" id="login-form">
                <% if (errorMessage) { %>
                    <div class="user-message user-messsage--error"><%= errorMessage %></div>
                <% } %>
                <h1>Login</h1>
                <div class="input-container">
                    <input type="text" name="username" id="username_text" placeholder="Username" value="<%= oldInput.username %>">
                    <i class='bx bxs-user'></i>
                </div>
                <div class="input-container">
                    <input type="password" name="password" id="password_text" placeholder="Password" value="<%= oldInput.password %>">
                    <i class='bx bxs-lock-alt' ></i>
                </div>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit">Login</button>
                <p>
                    Don't have an account? <a href="/sign-up">Sign up</a>
                </p>
            </form>
        </main>
        <footer></footer>
    </body>
</html>